import styled from 'styled-components'

export const HeaderContainer = styled.header`
  display: flex;
  flew-direction: row;
  justify-content: space-between;

  overflow: hidden;
  padding: 2rem 10rem;
  top: 0;
  /*TODO Arrumar forma de deixar fixo na descida*/

  nav {
    display: flex;
    gap: 0.75rem;
  }
`
